import 'package:flutter/material.dart';
import 'package:flutter_application_1/widgets/flight_task.dart';

class CalculatedData extends StatelessWidget {
  final List<Point> points;

  const CalculatedData({super.key, required this.points});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
